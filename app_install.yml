---
- name: Install and start nginx at local node
  hosts: all

  tasks:
  - name: installing nginx
    yum:
     name: nginx
     state: installed
  - name: start and enable nginx
    service:
     name: nginx
     state: started
     enabled: true 
  - name: check nginx status
    command: systemctl status nginx
    register: nginx_status 
